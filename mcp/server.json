{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "ai.ordercore/ordercore-mcp",
  "description": "Commerce backend for AI agents: catalog, prices, inventory, orders, idempotent checkout.",
  "version": "0.2.17",
  "packages": [
    {
      "registryType": "npm",
      "identifier": "@ordercore/mcp",
      "version": "0.2.17",
      "transport": { "type": "stdio" },
      "environmentVariables": [
        {
          "name": "ORDERCORE_API_KEY",
          "description": "Optional; omit for a read-only sandbox, set a full key from ordercore.ai/bootstrap for checkout.",
          "isRequired": false,
          "isSecret": true,
          "format": "string"
        },
        {
          "name": "ORDERCORE_BASE_URL",
          "description": "API base URL (default https://api.ordercore.ai).",
          "isRequired": false,
          "isSecret": false,
          "format": "string"
        }
      ]
    }
  ]
}
